<div class="container">
  <% flash.each do |name, msg| %>
    <div class="<%= "#{alert_color(name)}"%>" role="alert">
      <%= msg %>
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    <% end %>
  </div>
</div>
